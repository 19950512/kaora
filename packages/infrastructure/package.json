{
  "name": "@kaora/infrastructure",
  "version": "1.0.0",
  "description": "Kaora Infrastructure Layer - Prisma, Database",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prisma:generate": "prisma generate --schema=./prisma/schema.prisma",
    "prisma:push": "prisma db push --schema=./prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema=./prisma/schema.prisma"
  },
  "dependencies": {
    "@kaora/domain": "workspace:*",
    "@prisma/client": "^6.14.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prisma": "^6.14.0",
    "typescript": "^5.0.0"
  }
}
